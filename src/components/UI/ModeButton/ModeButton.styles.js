import styled from '@emotion/styled';

export const Button = styled.img`
  cursor: pointer;
  width: 40px;
  margin-right: 10px;
  transition: transform 0.3s ease;
  :hover {
    transform: scale(1.3);
  }
`;
